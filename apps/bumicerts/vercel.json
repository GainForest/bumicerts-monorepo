{
  "buildCommand": "bun run graphql:codegen && bun run build",
  "ignoreCommand": "git fetch --depth=2 origin $VERCEL_GIT_COMMIT_SHA && git diff --quiet $VERCEL_GIT_COMMIT_SHA^ $VERCEL_GIT_COMMIT_SHA -- apps/bumicerts/"
}
