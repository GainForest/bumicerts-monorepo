/*
 * THIS FILE WAS GENERATED BY "@atproto/lex". DO NOT EDIT.
 */

import { l } from '@atproto/lex'
import * as CertifiedDefs from '../../../app/certified/defs.defs.ts'

const $nsid = 'org.hypercerts.fraction.transferEvent'

export { $nsid }

/** A single, immutable transfer event for a fraction of an activity claim. Records the sender, recipient, amount, and cryptographic proof of the transfer. */
type Main = {
  $type: 'org.hypercerts.fraction.transferEvent'

  /**
   * The AT-URI of the activity claim that this transfer event is associated with.
   */
  activityClaimUri: l.AtUriString

  /**
   * The DID of the sender. Omit this field if the sender wishes to remain anonymous.
   */
  from?: CertifiedDefs.Did

  /**
   * The DID of the recipient of the transfer.
   */
  to: CertifiedDefs.Did

  /**
   * The amount transferred as a numeric string (e.g. '1000.50').
   */
  amount: string

  /**
   * The fiat currency of the transfer (e.g. USD, EUR).
   */
  currency: 'USD' | 'EUR' | 'GBP' | 'CHF' | 'JPY' | l.UnknownString

  /**
   * The timestamp embedded in the signed message payload. This value is part of the data that was cryptographically signed and must match the signing time used during signature generation.
   */
  signedAt: l.DatetimeString

  /**
   * The nonce included in the signed message, used to prevent replay attacks.
   */
  nonce?: string

  /**
   * The EVM chain ID on which this transfer was signed (e.g. '1' for Ethereum mainnet, '42161' for Arbitrum).
   */
  chainId?: string

  /**
   * The EVM address of the signer, in checksummed hex format with a 0x prefix (e.g. 0xAbCd...1234). Required for signature verification.
   */
  signerEVMAddress: string

  /**
   * The cryptographic signature over the transfer message payload.
   */
  signature: string

  /**
   * Client-declared timestamp when this record was originally created.
   */
  createdAt: l.DatetimeString
}

export type { Main }

/** A single, immutable transfer event for a fraction of an activity claim. Records the sender, recipient, amount, and cryptographic proof of the transfer. */
const main = l.record<'any', Main>(
  'any',
  $nsid,
  l.object({
    activityClaimUri: l.string({ format: 'at-uri' }),
    from: l.optional(
      l.ref<CertifiedDefs.Did>((() => CertifiedDefs.did) as any),
    ),
    to: l.ref<CertifiedDefs.Did>((() => CertifiedDefs.did) as any),
    amount: l.string({ maxLength: 50 }),
    currency: l.string<{
      maxLength: 10
      knownValues: ['USD', 'EUR', 'GBP', 'CHF', 'JPY']
    }>({ maxLength: 10 }),
    signedAt: l.string({ format: 'datetime' }),
    nonce: l.optional(l.string({ maxLength: 256 })),
    chainId: l.optional(l.string({ maxLength: 256 })),
    signerEVMAddress: l.string({ maxLength: 42 }),
    signature: l.string({ maxLength: 256 }),
    createdAt: l.string({ format: 'datetime' }),
  }),
)

export { main }

export const $isTypeOf = /*#__PURE__*/ main.isTypeOf.bind(main),
  $build = /*#__PURE__*/ main.build.bind(main),
  $type = /*#__PURE__*/ main.$type
export const $assert = /*#__PURE__*/ main.assert.bind(main),
  $check = /*#__PURE__*/ main.check.bind(main),
  $cast = /*#__PURE__*/ main.cast.bind(main),
  $ifMatches = /*#__PURE__*/ main.ifMatches.bind(main),
  $matches = /*#__PURE__*/ main.matches.bind(main),
  $parse = /*#__PURE__*/ main.parse.bind(main),
  $safeParse = /*#__PURE__*/ main.safeParse.bind(main),
  $validate = /*#__PURE__*/ main.validate.bind(main),
  $safeValidate = /*#__PURE__*/ main.safeValidate.bind(main)
